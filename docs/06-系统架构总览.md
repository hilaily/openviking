# 06 - 系统架构总览

> 从整体视角理解 OpenViking 的模块划分和数据流向。

---

## 🏗️ 整体架构图

```
┌────────────────────────────────────────────────────────────────────────────┐
│                           OpenViking 系统架构                                │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         Client 层 (SDK/API)                           │  │
│  │     SyncOpenViking / AsyncOpenViking / HTTPClient                     │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    │                                       │
│                                    ▼                                       │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        Service Layer (服务层)                          │  │
│  │                                                                        │  │
│  │   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐              │  │
│  │   │ FSService│  │SearchSvc │  │SessionSvc│  │ResourceSvc│              │  │
│  │   │文件系统  │  │ 搜索服务 │  │ 会话管理 │  │ 资源导入 │              │  │
│  │   └──────────┘  └──────────┘  └──────────┘  └──────────┘              │  │
│  │                                                                        │  │
│  │   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐              │  │
│  │   │Relation  │  │ PackSvc  │  │DebugSvc  │  │   ...   │              │  │
│  │   │ 关联管理 │  │ 导入导出 │  │ 调试服务 │  │          │              │  │
│  │   └──────────┘  └──────────┘  └──────────┘  └──────────┘              │  │
│  │                                                                        │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    │                                       │
│           ┌────────────────────────┼────────────────────────┐            │
│           │                        │                        │            │
│           ▼                        ▼                        ▼            │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐       │
│  │    Retrieve       │  │     Session       │  │      Parse        │       │
│  │    检索模块       │  │    会话管理       │  │     解析模块      │       │
│  │                  │  │                  │  │                  │       │
│  │ - IntentAnalyzer │  │ - SessionCompressor│  │ - DocumentParser │       │
│  │ - Hierarchical   │  │ - MemoryExtractor │  │ - TreeBuilder    │       │
│  │   Retriever     │  │                  │  │ - SemanticQueue  │       │
│  │ - Reranker      │  │                  │  │                  │       │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘       │
│           │                     │                     │                 │
│           │                     │ 记忆提取            │                 │
│           │                     ▼                     │                 │
│           │            ┌──────────────────┐           │                 │
│           │            │   Compressor     │           │                 │
│           │            │   记忆压缩/去重   │           │                 │
│           │            └────────┬─────────┘           │                 │
│           │                     │                     │                 │
│           └─────────────────────┼─────────────────────┘                 │
│                                 ▼                                        │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                        Storage Layer (存储层)                         │  │
│  │                                                                      │  │
│  │   ┌─────────────────────────────────────────────────────────────┐    │  │
│  │   │                        VikingFS                              │    │  │
│  │   │              (URI 抽象层，屏蔽底层存储细节)                     │    │  │
│  │   └─────────────────────────────────────────────────────────────┘    │  │
│  │                                │                                     │  │
│  │            ┌───────────────────┼───────────────────┐                 │  │
│  │            ▼                   ▼                   ▼                 │  │
│  │   ┌───────────────┐   ┌───────────────┐   ┌───────────────┐           │  │
│  │   │     AGFS      │   │   向量数据库   │   │    Observer   │           │  │
│  │   │   内容存储     │   │   语义索引     │   │   事件监听    │           │  │
│  │   │  - L0/L1/L2   │   │  - URI 映射   │   │              │           │  │
│  │   │  - 多媒体     │   │  - 向量检索   │   │              │           │  │
│  │   └───────────────┘   └───────────────┘   └───────────────┘           │  │
│  │                                                                      │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                            │
└──────────────────────────────────────────────
```

---

## 📦 核心模块详解

### Client 层

用户直接交互的入口。

| 客户端 | 说明 | 适用场景 |
|--------|------|----------|
| `SyncOpenViking` | 同步客户端 | 简单脚本、单进程应用 |
| `AsyncOpenViking` | 异步客户端 | 高并发、Web 服务 |
| `HTTPClient` | HTTP 客户端 | 跨语言、分布式部署 |

### Service 层

业务逻辑处理，将业务与传输解耦。

| 服务 | 职责 | 关键方法 |
|------|------|----------|
| **FSService** | 文件系统操作 | `ls`, `mkdir`, `rm`, `mv`, `read`, `glob` |
| **SearchService** | 语义搜索 | `search`, `find` |
| **SessionService** | 会话管理 | `session`, `commit`, `delete` |
| **ResourceService** | 资源导入 | `add_resource`, `wait_processed` |
| **RelationService** | 关联管理 | `relations`, `link`, `unlink` |
| **PackService** | 导入导出 | `export_ovpack`, `import_ovpack` |
| **DebugService** | 调试 | `observer` |

### Retrieve（检索模块）

负责从存储中检索相关上下文。

| 组件 | 职责 |
|------|------|
| **IntentAnalyzer** | 意图分析，将用户查询转换为结构化查询 |
| **HierarchicalRetriever** | 目录递归检索 |
| **Reranker** | 重排序，提高准确性 |

### Session（会话模块）

管理对话和提取记忆。

| 组件 | 职责 |
|------|------|
| **SessionCompressor** | 压缩归档历史消息 |
| **MemoryExtractor** | 从消息中提取 6 种分类记忆 |
| **Compressor** | 记忆去重决策 |

### Parse（解析模块）

处理外部资源导入。

| 组件 | 职责 |
|------|------|
| **DocumentParser** | 解析各种文档格式 |
| **TreeBuilder** | 构建目录树结构 |
| **SemanticQueue** | 异步生成 L0/L1 语义信息 |

### Storage Layer

双层存储架构。

| 存储层 | 职责 | 内容 |
|--------|------|------|
| **VikingFS** | URI 抽象层 | 统一操作接口 |
| **AGFS** | 内容存储 | L0/L1/L2 完整内容 |
| **向量数据库** | 索引存储 | URI、向量、元数据 |
| **Observer** | 事件监听 | 状态变化通知 |

---

## 🔄 数据流概览

### 场景 1：添加资源

```
用户调用 client.add_resource(path)
        │
        ▼
┌─────────────────┐
│  ResourceService│  接收请求
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ DocumentParser  │  解析文档格式
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   TreeBuilder   │  构建目录树
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  AGFS (存储)    │  写入内容
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ SemanticQueue   │  异步生成 L0/L1
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  向量数据库     │  建立索引
└─────────────────┘
```

### 场景 2：检索上下文

```
用户调用 client.find(query)
        │
        ▼
┌─────────────────┐
│ SearchService   │  接收查询
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ IntentAnalyzer  │  意图分析 → TypedQuery(s)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│Hierarchical     │  目录递归检索
│Retriever        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Reranker     │  精排（可选）
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   返回结果     │  MatchedContext 列表
└─────────────────┘
```

### 场景 3：提交会话

```
用户调用 session.commit()
        │
        ▼
┌─────────────────┐
│ SessionService  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│SessionCompressor│  归档旧消息，生成摘要
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│MemoryExtractor  │  提取 6 种记忆
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Compressor    │  去重决策
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   写入存储      │  AGFS + 向量库
└─────────────────┘
```

---

## 🎯 设计原则

| 原则 | 说明 | 实践 |
|------|------|------|
| **存储层纯粹** | 存储层只做存储和基础搜索 | Rerank 在检索层完成 |
| **三层信息** | L0/L1/L2 按需加载 | 节省 Token |
| **两阶段检索** | 向量召回 + Rerank 精排 | 提高准确性 |
| **单一数据源** | 所有内容从 AGFS 读取 | 向量库只存引用 |

---

## 📖 下一步

深入了解 OpenViking 的**双层存储架构**：

- [07 - 双层存储架构](./07-双层存储架构.md)
